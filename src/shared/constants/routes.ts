/** Every page this application serves. */
export enum ROUTES {
  EXPLORER = '/explorer',
  RANDOM_DECLARATIONS = '/random-declarations',
  STATISTICS = '/statistics',
}

/** Landing destination for the logo link and the not-found page. */
export const HOME_ROUTE = ROUTES.EXPLORER;
